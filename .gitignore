home/yolo/.gitconfig
home/yolo/.config
home/yolo/.claude/
home/yolo/.claude.json
home/yolo/.pi/agent/extensions/*/node_modules/
.env
.env.*

# Local clones of the extension forks (pi-searxng, pi-subagents, candidates, etc.).
# They are separate repos, installed at image build via git clone — never vendor them here.
helperrepos/
